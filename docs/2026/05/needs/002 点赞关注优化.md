# 模板优化

## 发布更新推广链接后，根据模版生成静态文件html，模板要展示回复者称号、等级、点赞、关注、收藏、浏览人数等

### 相关字段
- 问题表 qa_question：
  `follow_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '关注数',
  `look_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '浏览数',
  `favorite_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '收藏数',
  `like_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '点赞数',
- 回答表 qa_answer：
  `title_name` VARCHAR(50) NULL COMMENT '回复者称号',
  `level` INT UNSIGNED NULL COMMENT '等级',
  `follow_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '关注数',
  `favorite_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '收藏数',
  `like_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '点赞数',
  `time_text` VARCHAR(50) NULL COMMENT '时间显示文本（如：一个月前）',
- 回复表 qa_reply：
  `title_name` VARCHAR(50) NULL COMMENT '回答者称号',
  `level` INT UNSIGNED NULL COMMENT '等级',
  `follow_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '关注数',
  `favorite_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '收藏数',
  `like_count` INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '点赞数',
  `time_text` VARCHAR(50) NULL COMMENT '时间显示文本（如：一个月前）',

## 用户点击 点赞、关注、收藏 对应的数据要临时+1，刷新页面以后，就还原了

# 涉及文件
模板目录：/Users/wangjingjun/work/promotion/server/uploads/template/mobile/template-01
发布更新推广链接函数位置：/Users/wangjingjun/work/promotion/server/service/promotion/link_service.go func (s *LinkService) PublishPromotionLink(linkId uint) error {

