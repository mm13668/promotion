# 需求
OCPC回传转化类型标记


		landingVisitPublicRouter.POST("reportCopy", promoApi.ReportCopy) //复制号码
		landingMessagePublicRouter.POST("create", promoApi.CreateLandingMessage) //提交表单
		landingPhonePublicRouter.POST("create", promoApi.CreateLandingPhone) //注册转化

表 landing_visits 新增转化类型字段，记录用户访问触发的转化类型，目前只有上面3种

对应的值是 35 3 49
<el-option label="微信复制按钮点击" :value="35" />
<el-option label="表单提交成功" :value="3" />
<el-option label="注册激活后登录（注册转化）" :value="49" />

定时器自动回传时，需要判断访问记录是否触发到对应的转化类型，才能进行自动回传
// autoOcpcCallback 自动OCPC回传任务
func autoOcpcCallback(db *gorm.DB) {

## 说明
涉及到修改的sql，要写在一个文件给我，方便上线，放在 /Users/wangjingjun/work/promotion/docs/plan 下