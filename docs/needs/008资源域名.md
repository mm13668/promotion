# 需求
资源域名和api域名配置化：在后台支持配置

## 实现
在/Users/wangjingjun/work/promotion/server/uploads目录下的 /api/ 前面拼接对应的域名，然后后台点击更新，直接生成静态文件html时候，带上了对应的域名



后端需要提前 import "github.com/flipped-aurora/gin-vue-admin/server/service/system"

然后调用 new(system.SysParamsService).GetSysParam("") 来获取对应的 value 值。


图片等资源域名路径：new(system.SysParamsService).GetSysParam("source_domain_url")
落地页api接口请求url路径：new(system.SysParamsService).GetSysParam("h5_api_url")

发布更新推广链接函数位置：/Users/wangjingjun/work/promotion/server/service/promotion/link_service.go 
func (s *LinkService) PublishPromotionLink(linkId uint) error {


请求发布推广链接时，需要把 图片等资源域名路径 和 落地页api接口请求url路径 传到模板和插件中使用，然后在 /api/ 前面拼接对应的域名